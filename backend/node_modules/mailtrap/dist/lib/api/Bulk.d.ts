import { AxiosInstance } from "axios";
import { Mail, SendResponse } from "mailtrap/dist/types/mailtrap";
export default class BulkAPI {
    private client;
    constructor(client: AxiosInstance);
    send(mail: Mail): Promise<SendResponse>;
}
